name: lab2_flutter

description: "A new Flutter project."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.5.3

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8

  # Додано пакет shared_preferences
  shared_preferences: ^2.0.0  # Використовуйте останню версію

  # Додайте пакет crypto
  crypto: ^3.0.0  # Додайте цей рядок

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
